import { css } from '@emotion/react';

const styles = css({
  width: '100%',
  height: '100%',
  height: '100vh',
  overFlow: 'scroll',
  '.body': {
    '.session': {
      border: 'var(--border-secondary)',
      fontSize: '10px',
    },
  },
});

export default styles;
